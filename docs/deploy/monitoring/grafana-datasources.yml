apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false

  # 可选:同时接 SLS 的日志源(需要 Grafana Aliyun SLS 插件)
  # - name: SLS
  #   type: aliyun-logservice
  #   access: proxy
  #   url: https://fy-api-prod.cn-hangzhou.log.aliyuncs.com
  #   secureJsonData:
  #     accessKey: ...
  #     secretKey: ...
